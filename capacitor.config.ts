import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mbisurgi.distribuidoras",
  appName: "Stock",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
